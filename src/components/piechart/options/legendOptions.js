const legendOptions = {
    floating: false,
    itemMarginTop: 10,
    maxHeight: 80,
    width: 300,
    navigation: {
        activeColor: '#a7a7a7'
    }
}

const buildLegendOptions = () => {
    return legendOptions
}

export default buildLegendOptions
